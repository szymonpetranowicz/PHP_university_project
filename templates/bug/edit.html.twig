{% include 'default/_form.html.twig' with {
    page_title: 'title.bug.edit'|trans({'%id%': bug.id|default('')}),
    form: form,
    submit_label: 'edit'|trans,
    back_to_list_path: 'bug_index',
} %}