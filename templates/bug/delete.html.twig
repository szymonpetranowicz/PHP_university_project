{% include 'default/_form.html.twig' with {
    page_title: 'title.bug.delete'|trans({'%id%': bug.id|default('')}),
    form: form,
    submit_label: 'delete'|trans,
    back_to_list_path: 'bug_index',
} %}