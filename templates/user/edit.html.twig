{% include 'default/_form.html.twig' with {
    page_title: 'Edit password'|trans({'%id%': user.id|default('')}),
    form: form,
    submit_label: 'Edit'|trans,
    back_to_list_path: 'user_index',
} %}